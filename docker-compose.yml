version: '3.8'

services:
  client-reports:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: client-reports
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - NODE_ENV=production 